name: Build VideoQC App

on: [push]

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        # 同时在三个系统上运行
        os: [windows-latest, ubuntu-latest, macos-latest]

    steps:
    - uses: actions/checkout@v4  # 已更新为 v4
    
    # --- 强制使用 Python 3.8 以兼容 Windows 7 ---
    - name: Set up Python
      uses: actions/setup-python@v5  # 已更新为 v5
      with:
        python-version: '3.8' 
        
    - name: Install dependencies
      run: |
        pip install -r requirements.txt

    # --- Windows 打包命令 (分隔符用 ;) ---
    - name: Build EXE (Windows)
      if: runner.os == 'Windows'
      run: |
        pyinstaller --noconfirm --onefile --windowed --add-data "static;static" --name "VideoQC_Win7_10_11" app.py

    # --- Linux 打包命令 (分隔符用 :) ---
    - name: Build Binary (Linux)
      if: runner.os == 'Linux'
      run: |
        pyinstaller --noconfirm --onefile --windowed --add-data "static:static" --name "VideoQC_Linux" app.py

    # --- macOS 打包命令 (分隔符用 :) ---
    - name: Build App (macOS)
      if: runner.os == 'macOS'
      run: |
        pyinstaller --noconfirm --onefile --windowed --add-data "static:static" --name "VideoQC_Mac" app.py

    # --- 上传结果 (已修复：升级到 v4) ---
    - name: Upload Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: VideoQC-${{ matrix.os }}
        path: dist/
        # v4 版本默认不压缩，为了方便下载，我们保持默认即可，GitHub 界面下载时会自动打包成 zip
